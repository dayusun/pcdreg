% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{pcdreg-tidiers}
\alias{pcdreg-tidiers}
\alias{tidy.pcdfit}
\alias{glance.pcdfit}
\alias{augment.pcdfit}
\title{Tidy a panel count model fit}
\usage{
\method{tidy}{pcdfit}(
  x,
  conf.int = FALSE,
  conf.level = 0.95,
  exponentiate = FALSE,
  type = c("robust", "information", "profile"),
  ...
)

\method{glance}{pcdfit}(x, ...)

\method{augment}{pcdfit}(x, data = NULL, newdata = NULL, ...)
}
\arguments{
\item{x}{A fitted \code{\link[=pcdreg]{pcdreg()}} model.}

\item{conf.int}{Whether to add \code{conf.low} and \code{conf.high} columns.}

\item{conf.level}{Width of the interval, if one is requested.}

\item{exponentiate}{Whether to report \eqn{e^{\beta}} rather than
\eqn{\beta}. For the rate model this is the multiplicative effect of a
one unit change in the covariate on the rate, read the same way as a
hazard ratio; for the means model it is the effect on the mean. Standard
errors are left on the log scale, as elsewhere in the tidy ecosystem.}

\item{type}{Which covariance estimator to base the standard errors on. See
\code{\link[=vcov.pcdfit]{vcov.pcdfit()}}; the default is the robust sandwich.}

\item{...}{Ignored.}

\item{data, newdata}{Data to augment. Defaults to the data the model was
fitted to. It must contain the \code{\link[=pcd]{pcd()}} response, since the fitted mean
follows each subject's covariate trajectory.}
}
\value{
\code{tidy()} gives one row per coefficient, with columns \code{term}, \code{estimate},
\code{std.error}, \code{statistic} and \code{p.value}.

\code{glance()} gives a one row summary of the fit. \code{logLik} is \code{NA} for the
means model, which is fitted by an estimating equation and so has none.

\code{augment()} returns \code{data} with four columns added:
\describe{
\item{\code{.linear.predictor}}{\eqn{\beta' X(t)} on each row.}
\item{\code{.fitted}}{the predicted mean number of events by the end of the
row's interval.}
\item{\code{.observed}}{the observed cumulative count at that time, on
examination rows, and \code{NA} on rows that only record a covariate change.}
\item{\code{.resid}}{\code{.observed - .fitted}, the difference between the counts
seen so far and the mean the model implies.}
}
}
\description{
Methods for the \code{\link[generics:tidy]{generics::tidy()}}, \code{\link[generics:glance]{generics::glance()}} and
\code{\link[generics:augment]{generics::augment()}} generics, so that a \code{\link[=pcdreg]{pcdreg()}} fit can be handled the
same way as any other model in a tidy workflow.
}
\examples{
set.seed(1)
d <- sim_pcd(60, beta = c(1, -1), lambda = function(t) 8 / (1 + t))
fit <- pcdreg(pcd(id, tstart, tstop, count) ~ x1 + x2, data = d)

tidy(fit)
tidy(fit, conf.int = TRUE, exponentiate = TRUE)
glance(fit)
augment(fit)

}
