% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/panelrate.R
\name{panelrate}
\alias{panelrate}
\title{Fit the proportional rate model to panel count data}
\usage{
panelrate(
  formula,
  data,
  subset,
  na.action,
  control = panelrate_control(),
  profile = FALSE,
  init = NULL
)
}
\arguments{
\item{formula}{A formula whose left hand side is a \code{\link[=PanelCount]{PanelCount()}} object,
for example \code{PanelCount(id, tstart, tstop, count) ~ x1 + x2}. No intercept
is fitted: it is absorbed into the baseline \eqn{\Lambda}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{subset, na.action}{Handled as in \code{\link[stats:lm]{stats::lm()}}. Note that missing
covariate values delete rows, which can break the contiguity of a subject's
intervals; resolve them before fitting.}

\item{control}{A list from \code{\link[=panelrate_control]{panelrate_control()}}.}

\item{profile}{Whether to also compute the profile likelihood covariance.
This is provided for comparison with the literature and costs one extra
baseline-only fit per coefficient, so it is off by default.}

\item{init}{Optional starting values for the coefficients.}
}
\value{
An object of class \code{"panelrate"}, with components including
\code{coefficients}, \code{baseline} (a data frame of jump sizes and the cumulative
baseline rate), \code{Omega} and \code{S} (the two matrices of Section 4 of the
paper), \code{vcov} (a list of the available covariance estimates), \code{loglik},
and convergence information.
}
\description{
Fits
\deqn{E[dN(t) \mid X(t)] = \exp(\beta' X(t)) \, d\Lambda(t)}
by nonparametric maximum likelihood, treating \eqn{\Lambda} as a step
function with a jump at each observed examination time. Covariates may vary
over time.
}
\details{
The likelihood maximised is the one a nonhomogeneous Poisson process would
give. That assumption is a working device: it makes the EM algorithm
available, but the estimator remains consistent and asymptotically normal
when it fails, and the default covariance estimator remains valid.

Three covariance estimators are available through \code{\link[=vcov.panelrate]{vcov.panelrate()}}.
\code{"robust"} is the sandwich \eqn{\Omega^{-1} S \Omega^{-1} / n}, which does not
rely on the Poisson assumption and is the default. \code{"information"} is
\eqn{S^{-1} / n}, valid only under that assumption but a fast substitute for
the profile likelihood when it holds. \code{"profile"} is the numerical profile
likelihood estimator of Murphy and van der Vaart (2000), computed only when
\code{profile = TRUE}. When the counts are overdispersed relative to Poisson, the
latter two understate the standard errors, sometimes severely.

The profile estimator is included for comparison with the literature rather
than for routine use. It rests on a numerical derivative taken with a step of
order \eqn{n^{-1/2}}, which is coarse at small sample sizes, and it costs one
baseline-only fit per coefficient. \code{"information"} estimates the same matrix
far more cheaply and more stably.
}
\examples{
set.seed(1)
d <- r_panel_count(80, beta = c(1, -1), lambda = function(t) 8 / (1 + t))
fit <- panelrate(PanelCount(id, tstart, tstop, count) ~ x1 + x2, data = d)
fit
summary(fit)

}
\references{
Sun, D., Guo, Y., Li, Y., Tu, W. and Sun, J. (2024).
A robust approach for regression analysis of panel count data.
\emph{Bernoulli} \strong{30}(4), 3251--3275. \doi{10.3150/23-BEJ1713}

Murphy, S. A. and van der Vaart, A. W. (2000). On profile likelihood.
\emph{Journal of the American Statistical Association} \strong{95}, 449--465.
}
\seealso{
\code{\link[=PanelCount]{PanelCount()}}, \code{\link[=vcov.panelrate]{vcov.panelrate()}}, \code{\link[=r_panel_count]{r_panel_count()}}
}
