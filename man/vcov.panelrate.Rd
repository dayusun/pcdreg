% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{vcov.panelrate}
\alias{vcov.panelrate}
\title{Covariance matrix of the estimated coefficients}
\usage{
\method{vcov}{panelrate}(object, type = c("robust", "information", "profile"), ...)
}
\arguments{
\item{object}{A fitted \code{\link[=panelrate]{panelrate()}} model.}

\item{type}{Which estimator to return. \code{"robust"} is the sandwich estimator
that does not rely on the Poisson assumption; \code{"information"} is the
efficient information estimator, valid only under it; \code{"profile"} is the
profile likelihood estimator, available only if the model was fitted with
\code{profile = TRUE}.}

\item{...}{Ignored.}
}
\value{
A covariance matrix.
}
\description{
Covariance matrix of the estimated coefficients
}
\examples{
set.seed(1)
d <- r_panel_count(80, beta = c(1, -1), lambda = function(t) 8 / (1 + t))
fit <- panelrate(PanelCount(id, tstart, tstop, count) ~ x1 + x2, data = d)
sqrt(diag(vcov(fit)))
sqrt(diag(vcov(fit, "information")))
}
