% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_pcd}
\alias{sim_pcd}
\title{Simulate panel count data with a time-varying covariate}
\usage{
sim_pcd(
  n,
  beta = c(1, -1),
  lambda = function(t) 8/(1 + t),
  tau = 2,
  frailty = 0,
  exam_mean = 4,
  lambda_max = NULL,
  digits = 2
)
}
\arguments{
\item{n}{Number of subjects.}

\item{beta}{Length two coefficient vector for \code{x1} and \code{x2}.}

\item{lambda}{Baseline rate function, vectorised over \code{t}.}

\item{tau}{Maximum follow-up time.}

\item{frailty}{Variance of a gamma frailty with mean one that multiplies each
subject's rate. Zero, the default, gives a genuine Poisson process; a
positive value produces overdispersed counts that violate the Poisson
assumption while leaving the rate model intact.}

\item{exam_mean}{Mean of the zero-truncated Poisson distribution from which
the number of examinations per subject is drawn.}

\item{lambda_max}{Optional upper bound for \code{lambda} on \verb{[0, tau]}, used by the
thinning algorithm. The default takes the maximum over a fine grid, which
is reliable unless \code{lambda} oscillates very rapidly.}

\item{digits}{Number of decimal places to round examination times to, or
\code{NA} to leave them unrounded. Rounding follows the design of the paper and
of Wellner and Zhang (2007): examinations then tie across subjects, which
is realistic for scheduled visits and keeps the pooled grid, and hence the
cost of fitting, from growing quadratically in the sample size.}
}
\value{
A \link[tibble:tibble]{tibble} in counting process form with columns \code{id}, \code{tstart},
\code{tstop}, \code{count}, \code{x1} and \code{x2}. Rows are intervals over which the
covariates are constant; \code{count} is the number of events since the previous
examination and is \code{NA} on rows that only record a covariate change.
}
\description{
Generates data from the design used in the simulation study of Sun et al.
(2024), which is a convenient source of examples and test cases.
}
\details{
The time-varying covariate is a single step,
\eqn{x_1(t) = X_{11} I(t \le U_1) + X_{12} I(t > U_1)}, with
\eqn{X_{11}, X_{12} \sim U(0, 1)} and \eqn{U_1 \sim U(0, \tau)}. The
time-invariant covariate \code{x2} is \eqn{U(0, 1)}. Events follow a
nonhomogeneous Poisson process with rate
\eqn{r \lambda(t) \exp(\beta' X(t))}, where \eqn{r} is one unless \code{frailty}
is positive. Examination times are the order statistics of a uniform sample
on \eqn{(0, \bar T)} with \eqn{\bar T \sim U(0.9\tau, \tau)}.
}
\examples{
set.seed(42)
d <- sim_pcd(5, beta = c(1, -1), lambda = function(t) 8 / (1 + t))
head(d)

# Overdispersed counts: the rate model still holds, the Poisson one does not.
od <- sim_pcd(5, frailty = 1)
}
\references{
Sun, D., Guo, Y., Li, Y., Tu, W. and Sun, J. (2024).
A robust approach for regression analysis of panel count data.
\emph{Bernoulli} \strong{30}(4), 3251--3275. \doi{10.3150/23-BEJ1713}
}
